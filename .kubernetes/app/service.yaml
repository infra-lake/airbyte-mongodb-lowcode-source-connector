---
apiVersion: v1
kind: Service
metadata:
  namespace: airbyte-mongodb-lowcode-source
  name: app
spec:
  type: ClusterIP
  selector:
    app: app
  ports:
  - name: http
    port: 4000
    protocol: TCP
    targetPort: http